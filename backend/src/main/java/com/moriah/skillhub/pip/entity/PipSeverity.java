package com.moriah.skillhub.pip.entity;

/** Not spelled out in architecture.md — inferred as a simple three-tier scale, shared by {@code
 * pip_rules.severity} and {@code pip_records.severity} (V11). */
public enum PipSeverity {
    LOW,
    MEDIUM,
    HIGH
}
