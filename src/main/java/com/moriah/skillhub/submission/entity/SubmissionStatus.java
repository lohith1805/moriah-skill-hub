package com.moriah.skillhub.submission.entity;

/** Matches V7's {@code chk_task_submissions_status} CHECK constraint exactly. */
public enum SubmissionStatus {
    SUBMITTED,
    APPROVED,
    CHANGES_REQUESTED
}
