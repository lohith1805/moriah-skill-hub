package com.moriah.skillhub.hr.entity;

/** Mirrors the {@code chk_staff_attendance_status} CHECK constraint in {@code V36}. */
public enum StaffAttendanceStatus {
    PRESENT,
    LATE,
    ABSENT,
    HALF_DAY,
    ON_LEAVE
}
