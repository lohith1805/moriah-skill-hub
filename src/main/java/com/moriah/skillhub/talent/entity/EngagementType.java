package com.moriah.skillhub.talent.entity;

/** Mirrors {@code chk_recruitment_requests_engagement} in {@code V27__recruitment_requests.sql}. */
public enum EngagementType {
    FULL_TIME,
    CONTRACT,
    INTERNSHIP,
    PROJECT
}
