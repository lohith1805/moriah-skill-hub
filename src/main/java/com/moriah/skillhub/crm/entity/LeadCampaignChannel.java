package com.moriah.skillhub.crm.entity;

/** Mirrors {@code chk_lead_campaigns_channel} in {@code V24__lead_campaigns.sql}. */
public enum LeadCampaignChannel {
    EMAIL,
    SOCIAL,
    EVENT,
    REFERRAL,
    PAID_ADS,
    WEBINAR,
    OTHER
}
