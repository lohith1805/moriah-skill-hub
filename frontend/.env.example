# Base URL of the Spring Boot backend's REST API.
# Leave unset to use the Vite dev proxy (vite.config.js forwards /api -> http://localhost:8080).
# Set this to talk to a deployed backend directly, e.g.:
#   VITE_API_BASE_URL=https://api.moriahskillhub.com/api/v1
VITE_API_BASE_URL=
